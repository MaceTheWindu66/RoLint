#ifndef EXAMPLE_H
#define EXAMPLE_H

// Function declaration (allowed)
void print_message(const char* msg);

// Extern variable declaration (allowed)
extern int global_status;

// Macro definition (acceptable unless banned separately)
#define MAX_SIZE 100

#endif // EXAMPLE_H
